<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="Ie=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guia de Perguntas</title>
</head>
<body>
    <h1>Guia de Perguntas!</h1>
    <hr>
    <h3>Lista de perguntas</h3>

    <p>Nome: <%= name%></p>
    <p>Linguagem favorita: <%= lang%></p>
    <p>Empresa: <%= company%></p>
    <p>Inscritos no canal: <%= subscribes%></p>

    <% if(message){ %>
        <h3>Isso é uma mensagem de erro!</h3>
    <% } else{ %>
        <h3>Nenhum erro!</h3>
    <% } %>

    <% products.forEach(function(product){  %>
        <hr>
        <h4>Nome Produto: <%= product.name%></h4>
        <h5>Preco do Produto: <%= product.price%></h5>
    <% }) %>
        

</body>
</html>

<html>
    
</html>